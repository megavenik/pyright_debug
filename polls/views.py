from django.shortcuts import render
from polls.models import Tip

# Create your views here.

def main(request):
    all = Tip.objects.all()
    Tip.ti
    for i in all:
        print(i.)
    if request.use
